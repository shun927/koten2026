mediapipe==0.10.32
# ArUco lives in OpenCV contrib modules (cv2.aruco).
opencv-contrib-python==4.10.0.84
numpy==1.26.4
# RealSense (D435i etc.). Requires Intel RealSense SDK 2.0 / librealsense on Windows.
pyrealsense2
